from flask import Flask, render_template, jsonify, request\nfrom flask_cors import CORS\nfrom bot import TradingBot\nfrom config import Config\n\napp = Flask(__name__)\napp.config.from_object(Config)\nCORS(app)\n\n# Global bot instance\nbot = TradingBot()\nbot_started = False\n\n@app.route('/')\ndef index():\n    \"\"\"Ana sayfa\"\"\"\n    return render_template('index.html')\n\n@app.route('/api/start', methods=['POST'])\ndef start_bot():\n    \"\"\"Bot'u başlat\"\"\"\n    global bot_started\n    try:\n        if not bot_started:\n            bot.start()\n            bot_started = True\n        return jsonify({'success': True, 'message': 'Bot başlatıldı'})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/stop', methods=['POST'])\ndef stop_bot():\n    \"\"\"Bot'u durdur\"\"\"\n    global bot_started\n    try:\n        if bot_started:\n            bot.stop()\n            bot_started = False\n        return jsonify({'success': True, 'message': 'Bot durduruldu'})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/status')\ndef get_status():\n    \"\"\"Bot durumunu getir\"\"\"\n    return jsonify({\n        'running': bot.is_running,\n        'tracked_coins': bot.get_tracked_coins(),\n        'check_interval': app.config['CHECK_INTERVAL']\n    })\n\n@app.route('/api/add-coin', methods=['POST'])\ndef add_coin():\n    \"\"\"Kripto para ekle\"\"\"\n    try:\n        data = request.json\n        coin = data.get('coin', '').lower().strip()\n        \n        if not coin:\n            return jsonify({'success': False, 'message': 'Kripto para adı gerekli'}), 400\n        \n        bot.add_coin(coin)\n        return jsonify({'success': True, 'message': f'{coin} eklendi'})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/remove-coin/<coin>', methods=['DELETE'])\ndef remove_coin(coin):\n    \"\"\"Kripto para kaldır\"\"\"\n    try:\n        bot.remove_coin(coin.lower())\n        return jsonify({'success': True, 'message': f'{coin} kaldırıldı'})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/analysis')\ndef get_analysis():\n    \"\"\"Tüm kripto paralar için analiz getir\"\"\"\n    try:\n        analysis = bot.get_all_analysis()\n        return jsonify({\n            'success': True,\n            'data': analysis\n        })\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/analysis/<coin>')\ndef get_coin_analysis(coin):\n    \"\"\"Belirli bir kripto para için analiz getir\"\"\"\n    try:\n        analysis = bot.get_analysis(coin.lower())\n        if analysis:\n            return jsonify({'success': True, 'data': analysis})\n        else:\n            return jsonify({'success': False, 'message': 'Kripto para bulunamadı'}), 404\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/notifications')\ndef get_notifications():\n    \"\"\"Bildirimleri getir\"\"\"\n    try:\n        notifications = bot.notification_manager.get_history()\n        return jsonify({'success': True, 'data': notifications})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\n@app.route('/api/test-notification', methods=['POST'])\ndef test_notification():\n    \"\"\"Test bildirimi gönder\"\"\"\n    try:\n        bot.notification_manager.send_notification(\n            title=\"🤖 Test Bildirimi\",\n            message=\"Bu bir test bildirimidir.\",\n            signal=\"AL\",\n            coin=\"bitcoin\",\n            price=50000.00,\n            ema_short=49900.00,\n            ema_long=49800.00\n        )\n        return jsonify({'success': True, 'message': 'Test bildirimi gönderildi'})\n    except Exception as e:\n        return jsonify({'success': False, 'message': str(e)}), 500\n\nif __name__ == '__main__':\n    app.run(debug=True, host='0.0.0.0', port=5000)\n